// js/account-page.js – entry point for standalone account page
import { initApp, loadAccountPage } from "./common.js";

// Initialize Firebase listeners, auth state listeners, and DOM loaders
initApp();
loadAccountPage();
