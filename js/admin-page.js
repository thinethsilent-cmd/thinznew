// js/admin-page.js – entry point for standalone admin page
import { initApp, loadAdminPage } from "./common.js";

// Initialize Firebase listeners, auth state listeners, and DOM loaders
initApp();
loadAdminPage();
