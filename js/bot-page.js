// js/bot-page.js – entry point for standalone bot page
import { initApp, loadBotPage } from "./common.js";

// Initialize Firebase listeners, auth state listeners, and DOM loaders
initApp();
loadBotPage();
